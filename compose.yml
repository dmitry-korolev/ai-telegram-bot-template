services:
  bot:
    build:
      context: .
    env_file:
      - path: .env
        required: false
    volumes:
      - bot-data:/app/data
    ports:
      - "${HEALTH_PORT:-3000}:3000"

volumes:
  bot-data:
